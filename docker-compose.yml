services:
  pyai:
    image: pyai
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 80:80
    volumes:
      - /home/manav/Documents/pyai/tmp:/app/tmp
      - /home/manav/Documents/pyai/log:/app/log
    restart: unless-stopped

