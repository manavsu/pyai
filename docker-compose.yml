services:
  pyai:
    image: pyai
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 80:80
    volumes:
      - ./tmp:/home/manav/Documents/pyai/tmp
      - ./log:/home/manav/Documents/pyai/log
    restart: unless-stopped

